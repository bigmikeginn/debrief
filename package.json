{
  "name": "debrief",
  "version": "1.0.0",
  "description": "Training note archive and analysis",
  "private": true,
  "scripts": {
    "test": "node --test test/site-audit.test.mjs",
    "build": "echo 'Running minification and versioning...' && bash build.sh",
    "deploy": "bash deploy.sh"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}

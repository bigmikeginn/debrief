{
  "name": "debrief",
  "version": "1.0.0",
  "description": "Training note archive and analysis",
  "private": true,
  "scripts": {
    "build": "echo 'Assets are pre-built and committed to git'",
    "deploy": "bash deploy.sh"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
